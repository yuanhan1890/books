1.

```
2 3
3 -6

2 - λ   3
3      -6 - λ

det(A) = -(2 - λ)(6 + λ) - 9
 = - 12 - 2λ + 6λ + λ^2 - 9
 = λ^2 + 4λ - 21
 = (λ + 7)(λ - 3)

只有determinant为0时，(A - λI)x = 0才有非零解
```

5.

```
特征值为1

0.95 - 1   0.03
0.05       0.97 - 1

-0.05   0.03  x1  = 0
 0.05  -0.03  x2  = 0

x1       3
x2 =>    5

另外的特征值为0.92，特征向量为'[1, -1]

初始值x0为'[0.6, 0.4]

x0 = [v1, v2]('[c1, c2])

c1 = 0.125 c2 = 0.225

x0在特征向量空间中的坐标为0.125, 0.225

x1 = Ax0 = A[v1, v2]('[c1, c2])
= A * v1 * c1 + A * v2 * c2

A * v1 = 1 * v1
A * v2 = 0.92 * v2

x1 = c1 * v1 + 0.92 * c2 * v2

每次都乘A递增

xk = c1 * v1 + 0.92 ^ k * c2 * v2

k -> Infinity, xk = c1 * v2 = '[0.375, 0.625]
```